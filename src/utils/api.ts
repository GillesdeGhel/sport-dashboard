// All API calls should now use Supabase directly. These are placeholders to catch missed usages.

export function deprecatedApiCall() {
  throw new Error('apiService is deprecated. Use Supabase client directly.');
} 